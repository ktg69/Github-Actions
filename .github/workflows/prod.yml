name: Deploy on Main

on:
  push:
    branches:
      - main


jobs:
  deploy:
    runs-on: ubuntu-latest


    steps:
      - uses: actions/checkout@v3
        with:
            repository: https://github.com/ktg69/Workflow_test.git
      - run: echo "current directory is $(pwd)"      
      - run: bash bash.sh